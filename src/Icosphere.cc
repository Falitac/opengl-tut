#include "../include/Icosphere.hpp"

namespace generators {

void generateIcosphere(
  std::vector<float>& vertices,
  std::vector<uint32_t>& indices,
  float radius,
  int divisions
) {
  std::vector<float>(12 * 3).swap(vertices);



}

} //namespace generators